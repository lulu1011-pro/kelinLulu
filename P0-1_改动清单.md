# P0-1 改动清单

| 文件路径 | 改动类型 | 改动内容 | 动现有接口 |
|----------|----------|----------|------------|
| `src/database.cpp` | 修改 | `Init()` 中追加 `ai_conversations`、`ai_messages` 两张表的 `CREATE TABLE IF NOT EXISTS`（含软删字段、索引、外键），**不改现有表**，老库自动建新表零影响 | 否 |
| `src/database.h` | 修改 | 新增 `CreateAIConversation`、`GetAIConversations`、`GetAIConversation`、`DeleteAIConversation`、`UpdateAIConversation`、`AddAIMessage`、`GetAIMessages` 等 DB 操作声明 | 否 |
| `src/routes/ai_routes.h` | 修改 | 1. 顶部新增截断常量<br>2. 新增 `RegisterAIConversationRoutes()` 实现 5 个会话端点<br>3. 重构 `/api/ai/chat`：读 `conversation_id` → 会话模式（加锁、事务、读历史、截断、拼装、调模型、写两条消息、更新会话）<br>4. 新增会话级 `std::mutex` map + `sqlite3_busy_timeout` | **是**（`/api/ai/chat` 请求/响应体新增可选字段，兼容模式保持不变） |
| `src/utils/response.h` | 无需改 | 复用现有 `Success/Error` 格式 | 否 |
| `web/src/api.ts` | 修改 | 新增 `Conversation`、`ConversationDetail`、`ChatRequest`、`ChatResponse` 类型；新增 `conversationsApi`（create/list/get/delete/rename）；`chat` 接口入参/出参加 `conversation_id`/`message_id` | **是**（前端调用签名变更，旧调用无新字段仍兼容） |
| `web/src/components/AIChatPanel.vue` | 修改 | 重构为左右布局：左侧 `ConversationSidebar`（会话列表、新建/切换/删除/重命名），右侧 `ChatArea`（消息区+输入区）；状态提升到面板级（`conversations`、`activeConversationId`、`messages`）；`sendMessage` 带上 `conversation_id`；打开面板自动 `loadConversations` → 恢复/创建会话 → `loadMessages` | 否（组件内部重构，对外 `visible`/`close` props 不变） |
| `web/src/components/ConversationSidebar.vue` | 新增 | 会话列表侧边栏组件：列表渲染、新建按钮、重命名弹框、删除确认、点击切换、`updated_at` 相对时间显示 | 否 |
| `web/src/App.vue` | 修改 | 传递当前 `userId` 给 `AIChatPanel`（从 auth store 取），用于前端显示/调试，后端靠 JWT 鉴权 | 否 |

---

## 数据库迁移策略（零破坏老库）

**原则**：只加表、不改旧表、不删列、不改类型。

1. `ai_conversations`、`ai_messages` 为**全新表**，在 `Database::Init()` 追加：
   ```cpp
   Execute(R"(
       CREATE TABLE IF NOT EXISTS ai_conversations (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           user_id INTEGER NOT NULL,
           title TEXT NOT NULL DEFAULT '新对话',
           is_deleted INTEGER NOT NULL DEFAULT 0,
           deleted_at TEXT DEFAULT NULL,
           created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
           updated_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
           FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
       )
   )");
   Execute("CREATE INDEX IF NOT EXISTS idx_ai_conversations_user ON ai_conversations(user_id, updated_at DESC) WHERE is_deleted = 0");

   Execute(R"(
       CREATE TABLE IF NOT EXISTS ai_messages (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           conversation_id INTEGER NOT NULL,
           role TEXT NOT NULL CHECK (role IN ('user','assistant')),
           content TEXT NOT NULL,
           tokens INTEGER DEFAULT 0,
           created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
           FOREIGN KEY (conversation_id) REFERENCES ai_conversations(id) ON DELETE CASCADE
       )
   )");
   Execute("CREATE INDEX IF NOT EXISTS idx_ai_messages_conv ON ai_messages(conversation_id, created_at)");
   ```
   - `IF NOT EXISTS` 保证老库首次启动自动建表，新库同步建表，**无任何 ALTER、无数据迁移、无停机**。
   - 软删字段 `is_deleted`、`deleted_at` 在建表时一次性带上，后续无需再改表。

2. 现有 `notes`/`users` 等表**完全不动**，外键只从新表指向旧表，反向无依赖。

3. `Database` 构造函数已开启 `PRAGMA foreign_keys=ON`、`journal_mode=WAL`，新表自动受益。

---

## 并发写锁实现细节（`ai_routes.h` 内部）

```cpp
// 文件顶部静态变量
static std::mutex g_conv_mutex_map_mtx;
static std::unordered_map<int, std::unique_ptr<std::mutex>> g_conv_mutexes;

std::mutex& getConvMutex(int convId) {
    std::lock_guard<std::mutex> g(g_conv_mutex_map_mtx);
    auto it = g_conv_mutexes.find(convId);
    if (it == g_conv_mutexes.end()) {
        it = g_conv_mutexes.emplace(convId, std::make_unique<std::mutex>()).first;
    }
    return *it->second;
}

// /api/ai/chat 会话模式内部
if (conversation_id > 0) {
    std::mutex& mtx = getConvMutex(conversation_id);
    std::lock_guard<std::mutex> lock(mtx);

    db.Begin();
    try {
        // 1. 校验归属
        // 2. 读历史
        // 3. 截断拼装
        // 4. 调模型（网络 IO，锁仍持有——可接受，因模型调用通常 < 30s；若需优化可先拼好 prompt 再释放锁调模型，再加锁写入）
        // 5. 写 user/assistant 消息
        // 6. 更新会话 updated_at/title
        db.Commit();
    } catch (...) {
        db.Rollback(); throw;
    }
}
```
- 不同 `conversation_id` 互不阻塞
- 同一会话串行，避免 `SQLITE_BUSY`
- `sqlite3_busy_timeout(db, 5000)` 在 `Database` 构造函数已设置，兜底

---

## 验收自检项（写完代码自测用）

1. 旧前端不传 `conversation_id` → 单轮模式正常、不写会话表
2. 新前端传 `conversation_id` → 会话模式、历史生效、截断日志可观测
3. 用户 A 创建会话 → 用户 B 列表不可见、GET 404
4. 删除会话 → `is_deleted=1`、列表不显示、消息级联软删（或物理删，按实现）
5. 并发同一会话发两条请求 → 串行执行、无 `SQLITE_BUSY`、数据一致
6. 会话超 10 轮或超 8000 token → 早期历史自动丢弃、system+rag+question 保留